const body=document.body, theme=document.querySelector('.theme');theme.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('theme',body.classList.contains('dark')?'dark':'light')});if(localStorage.getItem('theme')==='dark')body.classList.add('dark');
const modal=document.querySelector('.modal'), title=document.querySelector('#modalTitle');document.querySelectorAll('.project-btn').forEach(b=>b.addEventListener('click',()=>{title.textContent=b.dataset.project;modal.classList.add('open');modal.setAttribute('aria-hidden','false')}));document.querySelector('.modal-close').addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}
const canvas=document.querySelector('#labCanvas'),ctx=canvas.getContext('2d');let dots=[],mouse={x:-9999,y:-9999};function resize(){const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);dots=Array.from({length:70},()=>({x:Math.random()*r.width,y:Math.random()*r.height,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45,r:1+Math.random()*2}))}window.addEventListener('resize',resize);canvas.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mouse.x=e.clientX-r.left;mouse.y=e.clientY-r.top});canvas.addEventListener('pointerleave',()=>{mouse.x=mouse.y=-9999});function draw(){const r=canvas.getBoundingClientRect();ctx.clearRect(0,0,r.width,r.height);for(const d of dots){const dx=mouse.x-d.x,dy=mouse.y-d.y,dist=Math.hypot(dx,dy);if(dist<130){d.vx+=dx/dist*.012;d.vy+=dy/dist*.012}d.x+=d.vx;d.y+=d.vy;if(d.x<0||d.x>r.width)d.vx*=-1;if(d.y<0||d.y>r.height)d.vy*=-1;ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fillStyle='#d7ff3f';ctx.fill();for(const e of dots){const ex=e.x-d.x,ey=e.y-d.y,ed=Math.hypot(ex,ey);if(ed<85){ctx.beginPath();ctx.moveTo(d.x,d.y);ctx.lineTo(e.x,e.y);ctx.strokeStyle='rgba(215,255,63,'+(1-ed/85)*.14+')';ctx.stroke()}}}requestAnimationFrame(draw)}resize();draw();